# Generic style
background-color=#282828
border-color=#d4be98
border-size=1
text-color=#d4be98
text-alignment=center
format=<b>%s</b>\n%b
default-timeout=5000
ignore-timeout=1

[urgency=low]
default-timeout=3000

#[urgency=normal]

[urgency=high]
border-color=#bf616a
border-size=2
default-timeout=0
anchor=center
format=<b>%a - %s</b>\n%b

# Spotify notifications are quite distracting while working
[app-name="Spotify"]
invisible=1

[app-name="system-info"]
border-size=2
default-timeout=0
height=10000
default-timeout=10000

# Zen mode (hide all notifications)
# Enable with `makoctl mode -a zen` disable with `makoctl mode -r zen`
[mode=zen]
invisible=1
