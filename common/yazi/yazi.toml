[opener]
open = [
	{ run = "xdg-open %s", orphan = true, desc = "Open", for = "unix" },
]
play = [
	{ run = "mpv %s", orphan = true, desc = "Play", for = "unix" },
]

[open]
prepend_rules = [
	{ mime = "video/*", use = [ "play", "open", "reveal" ] },
	{ mime = "audio/*", use = [ "play", "open", "reveal" ] },
]
